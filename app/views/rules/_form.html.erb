<%= form_for(rule) do |f| %>
  <% if rule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rule.errors.count, "error") %> prohibited this rule from being saved:</h2>

      <ul>
      <% rule.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :number %>
    <%= f.text_field :number %>
  </div>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :body %>
    <%= f.text_field :body %>
  </div>

  <div class="field">
    <%= f.label :violation_summary %>
    <%= f.text_field :violation_summary %>
  </div>

  <div class="field">
    <%= f.label :violation_recommondation %>
    <%= f.text_field :violation_recommondation %>
  </div>

  <div class="field">
    <%= f.label :thirty_day_summary %>
    <%= f.text_field :thirty_day_summary %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
