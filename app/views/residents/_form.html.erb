<%= form_for(resident) do |f| %>
  <% if resident.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(resident.errors.count, "error") %> prohibited this resident from being saved:</h2>

      <ul>
      <% resident.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :resident_one %>
    <%= f.text_field :resident_one %>
  </div>

  <div class="field">
    <%= f.label :resident_two %>
    <%= f.text_field :resident_two %>
  </div>

  <div class="field">
    <%= f.label :lot_number %>
    <%= f.text_field :lot_number %>
  </div>

  <div class="field">
    <%= f.label :address %>
    <%= f.text_field :address %>
  </div>

  <div class="field">
    <%= f.label :city %>
    <%= f.text_field :city %>
  </div>

  <div class="field">
    <%= f.label :state %>
    <%= f.text_field :state %>
  </div>

  <div class="field">
    <%= f.label :zip_code %>
    <%= f.text_field :zip_code %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
